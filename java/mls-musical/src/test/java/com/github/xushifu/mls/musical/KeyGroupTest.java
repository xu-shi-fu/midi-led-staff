package com.github.xushifu.mls.musical;

import org.junit.Test;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class KeyGroupTest {

    private static final Logger logger = LoggerFactory.getLogger(KeyGroupTest.class);

    @Test
    public void testGetGroups() {
        // KeyGroup[] groups = KeyGroup .  listGroups();
        // for (KeyGroup group : groups) {
        //     logger.debug(group + "");
        // }
    }

    @Test
    public void testGetGroup() {
        // for (int i = -3; i < 20; i++) {
        //     KeyGroup g = KeyGroup.getGroup(i);
        //     logger.debug("group[" + i + "] = " + g);
        // }
    }

}
